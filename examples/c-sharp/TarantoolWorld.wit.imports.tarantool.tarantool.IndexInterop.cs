// Generated by `wit-bindgen` 0.39.0. DO NOT EDIT!
// <auto-generated />
#nullable enable

using System.Runtime.InteropServices;
using System.Text;

namespace TarantoolWorld.wit.imports.tarantool.tarantool
{
    public static class IndexInterop {

        internal static class LenWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "len"), WasmImportLinkage]
            internal static extern void wasmImportLen(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe long Len(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            LenWasmInterop.wasmImportLen(index.id, index.spaceId, index.indexBase, ptr);

            Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class BsizeWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "bsize"), WasmImportLinkage]
            internal static extern void wasmImportBsize(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe long Bsize(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            BsizeWasmInterop.wasmImportBsize(index.id, index.spaceId, index.indexBase, ptr);

            Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class RandomWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "random"), WasmImportLinkage]
            internal static extern void wasmImportRandom(int p0, int p1, int p2, int p3, nint p4);
        }

        public  static unsafe ulong Random(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, uint rnd)
        {

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            RandomWasmInterop.wasmImportRandom(index.id, index.spaceId, index.indexBase, unchecked((int)(rnd)), ptr);

            Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class GetWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "get"), WasmImportLinkage]
            internal static extern void wasmImportGet(int p0, int p1, int p2, nint p3, int p4, nint p5);
        }

        public  static unsafe ulong Get(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, byte[] key)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            GetWasmInterop.wasmImportGet(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

            Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        public  static unsafe ulong Get(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Span<byte> key)
        {
            fixed (void* listPtr = key)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                GetWasmInterop.wasmImportGet(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        public  static unsafe ulong Get(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Memory<byte> key)
        {
            fixed (void* listPtr = key.Span)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                GetWasmInterop.wasmImportGet(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        internal static class MinWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "min"), WasmImportLinkage]
            internal static extern void wasmImportMin(int p0, int p1, int p2, nint p3, int p4, nint p5);
        }

        public  static unsafe ulong Min(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, byte[] key)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            MinWasmInterop.wasmImportMin(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

            Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        public  static unsafe ulong Min(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Span<byte> key)
        {
            fixed (void* listPtr = key)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                MinWasmInterop.wasmImportMin(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        public  static unsafe ulong Min(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Memory<byte> key)
        {
            fixed (void* listPtr = key.Span)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                MinWasmInterop.wasmImportMin(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        internal static class MaxWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "max"), WasmImportLinkage]
            internal static extern void wasmImportMax(int p0, int p1, int p2, nint p3, int p4, nint p5);
        }

        public  static unsafe ulong Max(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, byte[] key)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            MaxWasmInterop.wasmImportMax(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

            Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        public  static unsafe ulong Max(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Span<byte> key)
        {
            fixed (void* listPtr = key)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                MaxWasmInterop.wasmImportMax(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        public  static unsafe ulong Max(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Memory<byte> key)
        {
            fixed (void* listPtr = key.Span)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                MaxWasmInterop.wasmImportMax(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        internal static class CountWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "count"), WasmImportLinkage]
            internal static extern void wasmImportCount(int p0, nint p1, int p2, nint p3);
        }

        public  static unsafe long Count(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IteratorType iterType, byte[] key)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            CountWasmInterop.wasmImportCount((int)iterType, (nint)listPtr, (key).Length, ptr);

            Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted18;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted18 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index = 0; index < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted12;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted12 = null;
                            break;
                        }

                        case 1: {

                            lifted12 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted17;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted17 = null;
                            break;
                        }

                        case 1: {

                            lifted17 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted18 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted12, lifted17));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted18.IsOk)
            {
                var tmp = lifted18.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted18.AsErr!, 0);
            }

        }

        public  static unsafe long Count(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IteratorType iterType, Span<byte> key)
        {
            fixed (void* listPtr = key)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                CountWasmInterop.wasmImportCount((int)iterType, (nint)listPtr, (key).Length, ptr);

                Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted18;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted18 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index = 0; index < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted12;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted12 = null;
                                break;
                            }

                            case 1: {

                                lifted12 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted17;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted17 = null;
                                break;
                            }

                            case 1: {

                                lifted17 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted18 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted12, lifted17));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted18.IsOk)
                {
                    var tmp = lifted18.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted18.AsErr!, 0);
                }
            }

        }

        public  static unsafe long Count(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IteratorType iterType, Memory<byte> key)
        {
            fixed (void* listPtr = key.Span)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                CountWasmInterop.wasmImportCount((int)iterType, (nint)listPtr, (key).Length, ptr);

                Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted18;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted18 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index = 0; index < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted12;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted12 = null;
                                break;
                            }

                            case 1: {

                                lifted12 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted17;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted17 = null;
                                break;
                            }

                            case 1: {

                                lifted17 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted18 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted12, lifted17));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted18.IsOk)
                {
                    var tmp = lifted18.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted18.AsErr!, 0);
                }
            }

        }

        internal static class UniqueWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "unique"), WasmImportLinkage]
            internal static extern void wasmImportUnique(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe bool Unique(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            UniqueWasmInterop.wasmImportUnique(index.id, index.spaceId, index.indexBase, ptr);

            Result<bool, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<bool, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok((new Span<byte>((void*)(ptr + 4), 1)[0] != 0));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted19 = Result<bool, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class GetTypeWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "get-type"), WasmImportLinkage]
            internal static extern void wasmImportGetType(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexType GetType(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            GetTypeWasmInterop.wasmImportGetType(index.id, index.spaceId, index.indexBase, ptr);

            Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexType, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexType, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok((global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexType)new Span<byte>((void*)(ptr + 4), 1)[0]);
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted19 = Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexType, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class PartsWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "parts"), WasmImportLinkage]
            internal static extern void wasmImportParts(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef> Parts(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            PartsWasmInterop.wasmImportParts(index.id, index.spaceId, index.indexBase, ptr);

            Result<List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted38;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    var array = new List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)));
                    for (int index16 = 0; index16 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)); ++index16) {
                        nint basePtr1 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)) + (index16 * 48);

                        global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.FieldIdentifier lifted;

                        switch (new Span<byte>((void*)(basePtr1 + 0), 1)[0]) {
                            case 0: {

                                lifted = global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.FieldIdentifier.Number(unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 4), 4)))));
                                break;
                            }
                            case 1: {

                                lifted = global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.FieldIdentifier.Name(Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 8), 4))));
                                break;
                            }

                            default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(basePtr1 + 0), 1)[0]}");
                        }

                        string? lifted10;

                        switch (new Span<byte>((void*)(basePtr1 + 20), 1)[0]) {
                            case 0: {
                                lifted10 = null;
                                break;
                            }

                            case 1: {

                                lifted10 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 24), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 28), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(basePtr1 + 20), 1)[0]));
                        }

                        string? lifted15;

                        switch (new Span<byte>((void*)(basePtr1 + 32), 1)[0]) {
                            case 0: {
                                lifted15 = null;
                                break;
                            }

                            case 1: {

                                lifted15 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 36), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 40), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(basePtr1 + 32), 1)[0]));
                        }

                        array.Add(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef (
                        lifted, Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr1 + 16), 4))), lifted10, lifted15, (global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartFlags)(new Span<byte>((void*)(basePtr1 + 44), 1)[0])));
                    }

                    lifted38 = Result<List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(array);
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted27;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted27 = null;
                            break;
                        }

                        case 1: {

                            var array25 = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index26 = 0; index26 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index26) {
                                nint basePtr24 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index26 * 16);

                                array25.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr24 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr24 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr24 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr24 + 12), 4)))));
                            }

                            lifted27 = array25;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted32;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted32 = null;
                            break;
                        }

                        case 1: {

                            lifted32 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted37;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted37 = null;
                            break;
                        }

                        case 1: {

                            lifted37 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted38 = Result<List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted27, lifted32, lifted37));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted38.IsOk)
            {
                var tmp = lifted38.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted38.AsErr!, 0);
            }

        }

        internal static class PairsWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "pairs"), WasmImportLinkage]
            internal static extern void wasmImportPairs(int p0, int p1, int p2, int p3, nint p4, int p5, int p6, int p7, int p8, long p9, nint p10);
        }

        public  static unsafe global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Iterator Pairs(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.PairsOpts opts)
        {
            var cleanups = new List<Action>();

            int lowered;
            nint lowered3;
            int lowered4;

            if (opts.key != null) {
                var payload2 = (byte[]) opts.key;

                var gcHandle = GCHandle.Alloc(payload2, GCHandleType.Pinned);
                var listPtr = gcHandle.AddrOfPinnedObject();
                cleanups.Add(()=> gcHandle.Free());

                lowered = 1;
                lowered3 = (nint)listPtr;
                lowered4 = (payload2).Length;

            } else {

                lowered = 0;
                lowered3 = 0;
                lowered4 = 0;

            }

            int lowered11;
            int lowered12;

            if (opts.iterator != null) {
                var payload10 = (global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IteratorType) opts.iterator;

                lowered11 = 1;
                lowered12 = (int)payload10;

            } else {

                lowered11 = 0;
                lowered12 = 0;

            }

            int lowered19;
            long lowered20;

            if (opts.after != null) {
                var payload18 = (long) opts.after;

                lowered19 = 1;
                lowered20 = payload18;

            } else {

                lowered19 = 0;
                lowered20 = 0L;

            }

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            PairsWasmInterop.wasmImportPairs(index.id, index.spaceId, index.indexBase, lowered, lowered3, lowered4, lowered11, lowered12, lowered19, lowered20, ptr);

            Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Iterator, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted42;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {
                    var resource = new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Iterator(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Iterator.THandle(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4))));

                    lifted42 = Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Iterator, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(resource);
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index31 = 0; index31 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index31) {
                                nint basePtr30 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index31 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr30 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr30 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr30 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr30 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted36;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted36 = null;
                            break;
                        }

                        case 1: {

                            lifted36 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted41;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted41 = null;
                            break;
                        }

                        case 1: {

                            lifted41 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted42 = Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Iterator, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted36, lifted41));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted42.IsOk)
            {
                var tmp = lifted42.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted42.AsErr!, 0);
            }

        }

        internal static class SelectWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "select"), WasmImportLinkage]
            internal static extern void wasmImportSelect(int p0, int p1, int p2, nint p3, int p4, int p5, int p6, int p7, int p8, int p9, int p10, int p11, long p12, int p13, int p14, nint p15);
        }

        public  static unsafe List<ulong> Select(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, byte[] key, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.SelectOpts opts)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            int lowered;
            int lowered3;

            if (opts.iterator != null) {
                var payload2 = (global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IteratorType) opts.iterator;

                lowered = 1;
                lowered3 = (int)payload2;

            } else {

                lowered = 0;
                lowered3 = 0;

            }

            int lowered10;
            int lowered11;

            if (opts.limit != null) {
                var payload9 = (uint) opts.limit;

                lowered10 = 1;
                lowered11 = unchecked((int)(payload9));

            } else {

                lowered10 = 0;
                lowered11 = 0;

            }

            int lowered18;
            int lowered19;

            if (opts.offset != null) {
                var payload17 = (uint) opts.offset;

                lowered18 = 1;
                lowered19 = unchecked((int)(payload17));

            } else {

                lowered18 = 0;
                lowered19 = 0;

            }

            int lowered26;
            long lowered27;

            if (opts.after != null) {
                var payload25 = (long) opts.after;

                lowered26 = 1;
                lowered27 = payload25;

            } else {

                lowered26 = 0;
                lowered27 = 0L;

            }

            int lowered34;
            int lowered35;

            if (opts.fetchPos != null) {
                var payload33 = (bool) opts.fetchPos;

                lowered34 = 1;
                lowered35 = (payload33 ? 1 : 0);

            } else {

                lowered34 = 0;
                lowered35 = 0;

            }

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            SelectWasmInterop.wasmImportSelect(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, lowered, lowered3, lowered10, lowered11, lowered18, lowered19, lowered26, lowered27, lowered34, lowered35, ptr);

            Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted61;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    var array = new List<ulong>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)));
                    for (int index40 = 0; index40 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)); ++index40) {
                        nint basePtr39 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)) + (index40 * 8);

                        array.Add(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(basePtr39 + 0), 8)))));
                    }

                    lifted61 = Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(array);
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array49 = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index50 = 0; index50 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index50) {
                                nint basePtr48 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index50 * 16);

                                array49.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 12), 4)))));
                            }

                            lifted = array49;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted55;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted55 = null;
                            break;
                        }

                        case 1: {

                            lifted55 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted60;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted60 = null;
                            break;
                        }

                        case 1: {

                            lifted60 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted61 = Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted55, lifted60));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted61.IsOk)
            {
                var tmp = lifted61.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted61.AsErr!, 0);
            }

        }

        public  static unsafe List<ulong> Select(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Span<byte> key, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.SelectOpts opts)
        {
            fixed (void* listPtr = key)
            {

                int lowered;
                int lowered3;

                if (opts.iterator != null) {
                    var payload2 = (global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IteratorType) opts.iterator;

                    lowered = 1;
                    lowered3 = (int)payload2;

                } else {

                    lowered = 0;
                    lowered3 = 0;

                }

                int lowered10;
                int lowered11;

                if (opts.limit != null) {
                    var payload9 = (uint) opts.limit;

                    lowered10 = 1;
                    lowered11 = unchecked((int)(payload9));

                } else {

                    lowered10 = 0;
                    lowered11 = 0;

                }

                int lowered18;
                int lowered19;

                if (opts.offset != null) {
                    var payload17 = (uint) opts.offset;

                    lowered18 = 1;
                    lowered19 = unchecked((int)(payload17));

                } else {

                    lowered18 = 0;
                    lowered19 = 0;

                }

                int lowered26;
                long lowered27;

                if (opts.after != null) {
                    var payload25 = (long) opts.after;

                    lowered26 = 1;
                    lowered27 = payload25;

                } else {

                    lowered26 = 0;
                    lowered27 = 0L;

                }

                int lowered34;
                int lowered35;

                if (opts.fetchPos != null) {
                    var payload33 = (bool) opts.fetchPos;

                    lowered34 = 1;
                    lowered35 = (payload33 ? 1 : 0);

                } else {

                    lowered34 = 0;
                    lowered35 = 0;

                }

                var retArea = stackalloc uint[14+1];
                var ptr = ((int)retArea) + (4 - 1) & -4;
                SelectWasmInterop.wasmImportSelect(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, lowered, lowered3, lowered10, lowered11, lowered18, lowered19, lowered26, lowered27, lowered34, lowered35, ptr);

                Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted61;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        var array = new List<ulong>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)));
                        for (int index40 = 0; index40 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)); ++index40) {
                            nint basePtr39 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)) + (index40 * 8);

                            array.Add(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(basePtr39 + 0), 8)))));
                        }

                        lifted61 = Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(array);
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array49 = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                                for (int index50 = 0; index50 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index50) {
                                    nint basePtr48 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index50 * 16);

                                    array49.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 12), 4)))));
                                }

                                lifted = array49;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                        }

                        string? lifted55;

                        switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                            case 0: {
                                lifted55 = null;
                                break;
                            }

                            case 1: {

                                lifted55 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                        }

                        uint? lifted60;

                        switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                            case 0: {
                                lifted60 = null;
                                break;
                            }

                            case 1: {

                                lifted60 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                        }

                        lifted61 = Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted55, lifted60));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted61.IsOk)
                {
                    var tmp = lifted61.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted61.AsErr!, 0);
                }
            }

        }

        public  static unsafe List<ulong> Select(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Memory<byte> key, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.SelectOpts opts)
        {
            fixed (void* listPtr = key.Span)
            {

                int lowered;
                int lowered3;

                if (opts.iterator != null) {
                    var payload2 = (global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IteratorType) opts.iterator;

                    lowered = 1;
                    lowered3 = (int)payload2;

                } else {

                    lowered = 0;
                    lowered3 = 0;

                }

                int lowered10;
                int lowered11;

                if (opts.limit != null) {
                    var payload9 = (uint) opts.limit;

                    lowered10 = 1;
                    lowered11 = unchecked((int)(payload9));

                } else {

                    lowered10 = 0;
                    lowered11 = 0;

                }

                int lowered18;
                int lowered19;

                if (opts.offset != null) {
                    var payload17 = (uint) opts.offset;

                    lowered18 = 1;
                    lowered19 = unchecked((int)(payload17));

                } else {

                    lowered18 = 0;
                    lowered19 = 0;

                }

                int lowered26;
                long lowered27;

                if (opts.after != null) {
                    var payload25 = (long) opts.after;

                    lowered26 = 1;
                    lowered27 = payload25;

                } else {

                    lowered26 = 0;
                    lowered27 = 0L;

                }

                int lowered34;
                int lowered35;

                if (opts.fetchPos != null) {
                    var payload33 = (bool) opts.fetchPos;

                    lowered34 = 1;
                    lowered35 = (payload33 ? 1 : 0);

                } else {

                    lowered34 = 0;
                    lowered35 = 0;

                }

                var retArea = stackalloc uint[14+1];
                var ptr = ((int)retArea) + (4 - 1) & -4;
                SelectWasmInterop.wasmImportSelect(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, lowered, lowered3, lowered10, lowered11, lowered18, lowered19, lowered26, lowered27, lowered34, lowered35, ptr);

                Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted61;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        var array = new List<ulong>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)));
                        for (int index40 = 0; index40 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)); ++index40) {
                            nint basePtr39 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)) + (index40 * 8);

                            array.Add(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(basePtr39 + 0), 8)))));
                        }

                        lifted61 = Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(array);
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array49 = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                                for (int index50 = 0; index50 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index50) {
                                    nint basePtr48 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index50 * 16);

                                    array49.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr48 + 12), 4)))));
                                }

                                lifted = array49;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                        }

                        string? lifted55;

                        switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                            case 0: {
                                lifted55 = null;
                                break;
                            }

                            case 1: {

                                lifted55 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                        }

                        uint? lifted60;

                        switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                            case 0: {
                                lifted60 = null;
                                break;
                            }

                            case 1: {

                                lifted60 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                        }

                        lifted61 = Result<List<ulong>, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted55, lifted60));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted61.IsOk)
                {
                    var tmp = lifted61.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted61.AsErr!, 0);
                }
            }

        }

        internal static class UpdateWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "update"), WasmImportLinkage]
            internal static extern void wasmImportUpdate(int p0, int p1, int p2, nint p3, int p4, nint p5, int p6, nint p7);
        }

        public  static unsafe ulong Update(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, byte[] key, List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.UpdateOp> ops)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            void* address;
            if ((20 * ops.Count) < 1024) {
                var retArea = stackalloc uint[(5*ops.Count)+1];
                address = (void*)(((int)retArea) + (4 - 1) & -4);
            }
            else
            {
                var bufferSize = 20 * (nuint)ops.Count;
                address = NativeMemory.AlignedAlloc(bufferSize, 4);
                cleanups.Add(()=> NativeMemory.AlignedFree(address));
            }

            for (int index3 = 0; index3 < ops.Count; ++index3) {
                global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.UpdateOp element = ops[index3];
                int basePtr = (int)address + (index3 * 20);
                BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 0), 4), unchecked((int)(element.fieldNo)));

                var utf8Bytes = Encoding.UTF8.GetBytes(element.op);
                var length = utf8Bytes.Length;
                var gcHandle0 = GCHandle.Alloc(utf8Bytes, GCHandleType.Pinned);
                var interopString = gcHandle0.AddrOfPinnedObject();

                cleanups.Add(()=> gcHandle0.Free());
                BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 8), 4), length);
                BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 4), 4), interopString.ToInt32());

                var gcHandle2 = GCHandle.Alloc(element.operand, GCHandleType.Pinned);
                var listPtr1 = gcHandle2.AddrOfPinnedObject();
                cleanups.Add(()=> gcHandle2.Free());
                BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 16), 4), (element.operand).Length);
                BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 12), 4), (nint)listPtr1);

            }

            var retArea4 = stackalloc ulong[8+1];
            var ptr = ((int)retArea4) + (8 - 1) & -8;
            UpdateWasmInterop.wasmImportUpdate(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, (int)address, ops.Count, ptr);

            Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted26;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted26 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index15 = 0; index15 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index15) {
                                nint basePtr14 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index15 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted20;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted20 = null;
                            break;
                        }

                        case 1: {

                            lifted20 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted25;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted25 = null;
                            break;
                        }

                        case 1: {

                            lifted25 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted26 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted20, lifted25));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted26.IsOk)
            {
                var tmp = lifted26.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted26.AsErr!, 0);
            }

        }

        public  static unsafe ulong Update(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Span<byte> key, List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.UpdateOp> ops)
        {
            var cleanups = new List<Action>();
            fixed (void* listPtr = key)
            {

                void* address;
                if ((20 * ops.Count) < 1024) {
                    var retArea = stackalloc uint[(5*ops.Count)+1];
                    address = (void*)(((int)retArea) + (4 - 1) & -4);
                }
                else
                {
                    var bufferSize = 20 * (nuint)ops.Count;
                    address = NativeMemory.AlignedAlloc(bufferSize, 4);
                    cleanups.Add(()=> NativeMemory.AlignedFree(address));
                }

                for (int index3 = 0; index3 < ops.Count; ++index3) {
                    global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.UpdateOp element = ops[index3];
                    int basePtr = (int)address + (index3 * 20);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 0), 4), unchecked((int)(element.fieldNo)));

                    var utf8Bytes = Encoding.UTF8.GetBytes(element.op);
                    var length = utf8Bytes.Length;
                    var gcHandle0 = GCHandle.Alloc(utf8Bytes, GCHandleType.Pinned);
                    var interopString = gcHandle0.AddrOfPinnedObject();

                    cleanups.Add(()=> gcHandle0.Free());
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 8), 4), length);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 4), 4), interopString.ToInt32());

                    var gcHandle2 = GCHandle.Alloc(element.operand, GCHandleType.Pinned);
                    var listPtr1 = gcHandle2.AddrOfPinnedObject();
                    cleanups.Add(()=> gcHandle2.Free());
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 16), 4), (element.operand).Length);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 12), 4), (nint)listPtr1);

                }

                var retArea4 = stackalloc ulong[8+1];
                var ptr = ((int)retArea4) + (8 - 1) & -8;
                UpdateWasmInterop.wasmImportUpdate(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, (int)address, ops.Count, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted26;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted26 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index15 = 0; index15 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index15) {
                                    nint basePtr14 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index15 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted20;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted20 = null;
                                break;
                            }

                            case 1: {

                                lifted20 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted25;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted25 = null;
                                break;
                            }

                            case 1: {

                                lifted25 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted26 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted20, lifted25));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }

                foreach (var cleanup in cleanups)
                {
                    cleanup();
                }
                if (lifted26.IsOk)
                {
                    var tmp = lifted26.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted26.AsErr!, 0);
                }
            }

        }

        public  static unsafe ulong Update(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Memory<byte> key, List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.UpdateOp> ops)
        {
            var cleanups = new List<Action>();
            fixed (void* listPtr = key.Span)
            {

                void* address;
                if ((20 * ops.Count) < 1024) {
                    var retArea = stackalloc uint[(5*ops.Count)+1];
                    address = (void*)(((int)retArea) + (4 - 1) & -4);
                }
                else
                {
                    var bufferSize = 20 * (nuint)ops.Count;
                    address = NativeMemory.AlignedAlloc(bufferSize, 4);
                    cleanups.Add(()=> NativeMemory.AlignedFree(address));
                }

                for (int index3 = 0; index3 < ops.Count; ++index3) {
                    global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.UpdateOp element = ops[index3];
                    int basePtr = (int)address + (index3 * 20);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 0), 4), unchecked((int)(element.fieldNo)));

                    var utf8Bytes = Encoding.UTF8.GetBytes(element.op);
                    var length = utf8Bytes.Length;
                    var gcHandle0 = GCHandle.Alloc(utf8Bytes, GCHandleType.Pinned);
                    var interopString = gcHandle0.AddrOfPinnedObject();

                    cleanups.Add(()=> gcHandle0.Free());
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 8), 4), length);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 4), 4), interopString.ToInt32());

                    var gcHandle2 = GCHandle.Alloc(element.operand, GCHandleType.Pinned);
                    var listPtr1 = gcHandle2.AddrOfPinnedObject();
                    cleanups.Add(()=> gcHandle2.Free());
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 16), 4), (element.operand).Length);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 12), 4), (nint)listPtr1);

                }

                var retArea4 = stackalloc ulong[8+1];
                var ptr = ((int)retArea4) + (8 - 1) & -8;
                UpdateWasmInterop.wasmImportUpdate(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, (int)address, ops.Count, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted26;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted26 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index15 = 0; index15 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index15) {
                                    nint basePtr14 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index15 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr14 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted20;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted20 = null;
                                break;
                            }

                            case 1: {

                                lifted20 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted25;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted25 = null;
                                break;
                            }

                            case 1: {

                                lifted25 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted26 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted20, lifted25));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }

                foreach (var cleanup in cleanups)
                {
                    cleanup();
                }
                if (lifted26.IsOk)
                {
                    var tmp = lifted26.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted26.AsErr!, 0);
                }
            }

        }

        internal static class DeleteWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "delete"), WasmImportLinkage]
            internal static extern void wasmImportDelete(int p0, int p1, int p2, nint p3, int p4, nint p5);
        }

        public  static unsafe ulong Delete(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, byte[] key)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            DeleteWasmInterop.wasmImportDelete(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

            Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        public  static unsafe ulong Delete(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Span<byte> key)
        {
            fixed (void* listPtr = key)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                DeleteWasmInterop.wasmImportDelete(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        public  static unsafe ulong Delete(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Memory<byte> key)
        {
            fixed (void* listPtr = key.Span)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                DeleteWasmInterop.wasmImportDelete(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(unchecked((ulong)(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)))));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<ulong, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        internal static class AlterWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "alter"), WasmImportLinkage]
            internal static extern void wasmImportAlter(int p0, int p1, int p2, int p3, nint p4, int p5, nint p6);
        }

        public  static unsafe void Alter(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, bool unique, List<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef> parts)
        {
            var cleanups = new List<Action>();

            void* address;
            if ((48 * parts.Count) < 1024) {
                var retArea = stackalloc uint[(12*parts.Count)+1];
                address = (void*)(((int)retArea) + (4 - 1) & -4);
            }
            else
            {
                var bufferSize = 48 * (nuint)parts.Count;
                address = NativeMemory.AlignedAlloc(bufferSize, 4);
                cleanups.Add(()=> NativeMemory.AlignedFree(address));
            }

            for (int index29 = 0; index29 < parts.Count; ++index29) {
                global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.KeyPartDef element = parts[index29];
                int basePtr = (int)address + (index29 * 48);

                switch (element.field.Tag) {
                    case 0: {
                        uint payload = element.field.AsNumber;
                        *(byte*)(basePtr + 0) = (byte)0;
                        BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 4), 4), unchecked((int)(payload)));

                        break;
                    }
                    case 1: {
                        string payload4 = element.field.AsName;
                        *(byte*)(basePtr + 0) = (byte)1;

                        var utf8Bytes = Encoding.UTF8.GetBytes(payload4);
                        var length = utf8Bytes.Length;
                        var gcHandle = GCHandle.Alloc(utf8Bytes, GCHandleType.Pinned);
                        var interopString = gcHandle.AddrOfPinnedObject();

                        cleanups.Add(()=> gcHandle.Free());
                        BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 8), 4), length);
                        BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 4), 4), interopString.ToInt32());

                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {element.field}");
                }

                var utf8Bytes6 = Encoding.UTF8.GetBytes(element.fieldType);
                var length7 = utf8Bytes6.Length;
                var gcHandle8 = GCHandle.Alloc(utf8Bytes6, GCHandleType.Pinned);
                var interopString5 = gcHandle8.AddrOfPinnedObject();

                cleanups.Add(()=> gcHandle8.Free());
                BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 16), 4), length7);
                BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 12), 4), interopString5.ToInt32());

                if (element.collation != null) {
                    var payload14 = (string) element.collation;
                    *(byte*)(basePtr + 20) = (byte)1;

                    var utf8Bytes16 = Encoding.UTF8.GetBytes(payload14);
                    var length17 = utf8Bytes16.Length;
                    var gcHandle18 = GCHandle.Alloc(utf8Bytes16, GCHandleType.Pinned);
                    var interopString15 = gcHandle18.AddrOfPinnedObject();

                    cleanups.Add(()=> gcHandle18.Free());
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 28), 4), length17);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 24), 4), interopString15.ToInt32());

                } else {

                    *(byte*)(basePtr + 20) = (byte)0;

                }

                if (element.path != null) {
                    var payload24 = (string) element.path;
                    *(byte*)(basePtr + 32) = (byte)1;

                    var utf8Bytes26 = Encoding.UTF8.GetBytes(payload24);
                    var length27 = utf8Bytes26.Length;
                    var gcHandle28 = GCHandle.Alloc(utf8Bytes26, GCHandleType.Pinned);
                    var interopString25 = gcHandle28.AddrOfPinnedObject();

                    cleanups.Add(()=> gcHandle28.Free());
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 40), 4), length27);
                    BitConverter.TryWriteBytes(new Span<byte>((void*)(basePtr + 36), 4), interopString25.ToInt32());

                } else {

                    *(byte*)(basePtr + 32) = (byte)0;

                }
                *(byte*)(basePtr + 44) = (byte)(int)element.flags;

            }

            var retArea30 = stackalloc uint[14+1];
            var ptr = ((int)retArea30) + (4 - 1) & -4;
            AlterWasmInterop.wasmImportAlter(index.id, index.spaceId, index.indexBase, (unique ? 1 : 0), (int)address, parts.Count, ptr);

            Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted52;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted52 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(new global::TarantoolWorld.None());
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index41 = 0; index41 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index41) {
                                nint basePtr40 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index41 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr40 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr40 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr40 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr40 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted46;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted46 = null;
                            break;
                        }

                        case 1: {

                            lifted46 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted51;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted51 = null;
                            break;
                        }

                        case 1: {

                            lifted51 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted52 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted46, lifted51));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted52.IsOk)
            {
                var tmp = lifted52.AsOk;
                return ;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted52.AsErr!, 0);
            }

        }

        internal static class DropWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "drop"), WasmImportLinkage]
            internal static extern void wasmImportDrop(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe void Drop(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            DropWasmInterop.wasmImportDrop(index.id, index.spaceId, index.indexBase, ptr);

            Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(new global::TarantoolWorld.None());
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted19 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return ;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class RenameWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "rename"), WasmImportLinkage]
            internal static extern void wasmImportRename(int p0, int p1, int p2, nint p3, int p4, nint p5);
        }

        public  static unsafe void Rename(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, string newName)
        {
            var cleanups = new List<Action>();

            var utf8Bytes = Encoding.UTF8.GetBytes(newName);
            var length = utf8Bytes.Length;
            var gcHandle = GCHandle.Alloc(utf8Bytes, GCHandleType.Pinned);
            var interopString = gcHandle.AddrOfPinnedObject();

            cleanups.Add(()=> gcHandle.Free());

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            RenameWasmInterop.wasmImportRename(index.id, index.spaceId, index.indexBase, interopString.ToInt32(), length, ptr);

            Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(new global::TarantoolWorld.None());
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted19 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return ;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class StatWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "stat"), WasmImportLinkage]
            internal static extern void wasmImportStat(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexStat Stat(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            StatWasmInterop.wasmImportStat(index.id, index.spaceId, index.indexBase, ptr);

            Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexStat, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexStat, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexStat (
                    BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)), BitConverter.ToInt64(new Span<byte>((void*)(ptr + 16), 8))));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.IndexStat, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class CompactWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "compact"), WasmImportLinkage]
            internal static extern void wasmImportCompact(int p0, int p1, int p2, nint p3);
        }

        public  static unsafe void Compact(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index)
        {

            var retArea = stackalloc uint[14+1];
            var ptr = ((int)retArea) + (4 - 1) & -4;
            CompactWasmInterop.wasmImportCompact(index.id, index.spaceId, index.indexBase, ptr);

            Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(new global::TarantoolWorld.None());
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 24), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 28), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 24), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 36), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 40), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 36), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 48), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 52), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 48), 1)[0]));
                    }

                    lifted19 = Result<None, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return ;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        internal static class TuplePosWasmInterop
        {
            [DllImport("tarantool:tarantool/index", EntryPoint = "tuple-pos"), WasmImportLinkage]
            internal static extern void wasmImportTuplePos(int p0, int p1, int p2, nint p3, int p4, nint p5);
        }

        public  static unsafe long TuplePos(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, byte[] key)
        {
            var cleanups = new List<Action>();

            var gcHandle = GCHandle.Alloc(key, GCHandleType.Pinned);
            var listPtr = gcHandle.AddrOfPinnedObject();
            cleanups.Add(()=> gcHandle.Free());

            var retArea = stackalloc ulong[8+1];
            var ptr = ((int)retArea) + (8 - 1) & -8;
            TuplePosWasmInterop.wasmImportTuplePos(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

            Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

            switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                case 0: {

                    lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                    break;
                }
                case 1: {

                    List<(string, string)>? lifted;

                    switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                        case 0: {
                            lifted = null;
                            break;
                        }

                        case 1: {

                            var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                            for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                            }

                            lifted = array;
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                    }

                    string? lifted13;

                    switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                        case 0: {
                            lifted13 = null;
                            break;
                        }

                        case 1: {

                            lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                    }

                    uint? lifted18;

                    switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                        case 0: {
                            lifted18 = null;
                            break;
                        }

                        case 1: {

                            lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                            break;
                        }

                        default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                    }

                    lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                    Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                    break;
                }

                default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
            }

            foreach (var cleanup in cleanups)
            {
                cleanup();
            }
            if (lifted19.IsOk)
            {
                var tmp = lifted19.AsOk;
                return tmp;
            }
            else
            {
                throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
            }

        }

        public  static unsafe long TuplePos(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Span<byte> key)
        {
            fixed (void* listPtr = key)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                TuplePosWasmInterop.wasmImportTuplePos(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

        public  static unsafe long TuplePos(global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.Index index, Memory<byte> key)
        {
            fixed (void* listPtr = key.Span)
            {

                var retArea = stackalloc ulong[8+1];
                var ptr = ((int)retArea) + (8 - 1) & -8;
                TuplePosWasmInterop.wasmImportTuplePos(index.id, index.spaceId, index.indexBase, (nint)listPtr, (key).Length, ptr);

                Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError> lifted19;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Ok(BitConverter.ToInt64(new Span<byte>((void*)(ptr + 8), 8)));
                        break;
                    }
                    case 1: {

                        List<(string, string)>? lifted;

                        switch (new Span<byte>((void*)(ptr + 28), 1)[0]) {
                            case 0: {
                                lifted = null;
                                break;
                            }

                            case 1: {

                                var array = new List<(string, string)>(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)));
                                for (int index8 = 0; index8 < BitConverter.ToInt32(new Span<byte>((void*)(ptr + 36), 4)); ++index8) {
                                    nint basePtr7 = BitConverter.ToInt32(new Span<byte>((void*)(ptr + 32), 4)) + (index8 * 16);

                                    array.Add((Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 0), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 4), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(basePtr7 + 12), 4)))));
                                }

                                lifted = array;
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 28), 1)[0]));
                        }

                        string? lifted13;

                        switch (new Span<byte>((void*)(ptr + 40), 1)[0]) {
                            case 0: {
                                lifted13 = null;
                                break;
                            }

                            case 1: {

                                lifted13 = Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 44), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 48), 4)));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 40), 1)[0]));
                        }

                        uint? lifted18;

                        switch (new Span<byte>((void*)(ptr + 52), 1)[0]) {
                            case 0: {
                                lifted18 = null;
                                break;
                            }

                            case 1: {

                                lifted18 = unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 56), 4))));
                                break;
                            }

                            default: throw new ArgumentException("invalid discriminant: " + (new Span<byte>((void*)(ptr + 52), 1)[0]));
                        }

                        lifted19 = Result<long, global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>.Err(new global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError (
                        Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 12), 4))), Encoding.UTF8.GetString((byte*)BitConverter.ToInt32(new Span<byte>((void*)(ptr + 16), 4)), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 20), 4))), unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 24), 4)))), lifted, lifted13, lifted18));
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (lifted19.IsOk)
                {
                    var tmp = lifted19.AsOk;
                    return tmp;
                }
                else
                {
                    throw new WitException<global::TarantoolWorld.wit.imports.tarantool.tarantool.ITypes.BoxError>(lifted19.AsErr!, 0);
                }
            }

        }

    }
}
